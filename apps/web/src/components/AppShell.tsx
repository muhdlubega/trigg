import type { ReactNode } from 'react';
import type { User } from 'firebase/auth';
import { Activity, Blocks, ChevronDown, Github, LayoutDashboard, LogOut, Settings as SettingsIcon, Zap } from 'lucide-react';
import { NavLink, useNavigate } from 'react-router-dom';
import { logout, mock } from '../lib/api';
export function AppShell({user,children}:{user:User;children:ReactNode}){const navigate=useNavigate();const items=[['/dashboard','Overview',LayoutDashboard],['/workflows','Workflows',Blocks],['/executions','Executions',Activity],['/integrations','Integrations',Zap],['/settings','Settings',SettingsIcon]] as const;return <div className="app-shell"><aside className="sidebar"><a className="brand" href="/dashboard"><span className="logo-mark">T</span>Trigg <span className="beta">BETA</span></a><nav>{items.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>isActive?'active':''}><Icon size={17}/>{label}</NavLink>)}</nav><div className="sidebar-bottom"><div className="github-chip"><Github size={15}/><span>{mock?'Mock mode':'GitHub App'}</span><span className="status-dot"/></div><button className="profile" onClick={()=>void logout().then(()=>navigate('/'))}><span className="avatar">{(user.displayName??user.email??'T')[0]}</span><span><b>{user.displayName??'Developer'}</b><small>{user.email}</small></span><LogOut size={15}/><ChevronDown size={14}/></button></div></aside><main className="app-main">{children}</main></div>}
