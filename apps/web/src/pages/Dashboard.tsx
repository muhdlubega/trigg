import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { Activity, ArrowUpRight, Blocks, Bot, CheckCircle2, Github, Play, Plus, Sparkles } from 'lucide-react';
import { Link } from 'react-router-dom';
import { api } from '../lib/api';
import { PageHeader, Skeleton, Status } from '../components/UI';
type Data={activeWorkflows:number;executionsToday:number;successRate:number;aiRuns:number;aiTokens:number;recent:Array<Record<string,string|number>>;active:Array<{id:string;name:string;repository:string}>};
export function Dashboard(){const client=useQueryClient();const {data,isLoading}=useQuery({queryKey:['dashboard'],queryFn:()=>api<Data>('/api/dashboard')});const run=useMutation({mutationFn:(id:string)=>api<{pullRequest:number}>(`/api/workflows/${id}/run`,{method:'POST'}),onSuccess:()=>void client.invalidateQueries({queryKey:['dashboard']})});const hour=new Date().getHours();const metrics=[{Icon:Blocks,label:'Active workflows',value:data?.activeWorkflows??0,note:'Repository reviewers enabled'},{Icon:Activity,label:'Executions today',value:data?.executionsToday??0,note:'Webhook and manual runs'},{Icon:CheckCircle2,label:'Success rate today',value:`${data?.successRate??0}%`,note:'Completed executions'},{Icon:Bot,label:'AI runs today',value:data?.aiRuns??0,note:`${(data?.aiTokens??0).toLocaleString()} tokens`}];return <div className="page"><PageHeader eyebrow="OVERVIEW" title={`${hour<12?'Good morning':hour<18?'Good afternoon':'Good evening'} 👋`} description="Trigger a review now or let Trigg review new pull requests automatically." action={<Link to="/workflows" className="button primary"><Plus size={15}/>New reviewer</Link>}/><div className="metric-grid">{metrics.map(({Icon,label,value,note})=><article className="metric" key={label}><div><Icon size={17}/></div><small>{label}</small><strong>{value}</strong><p>{note}</p></article>)}</div><div className="dashboard-grid"><section className="panel"><header><div><h2>Repository workflows</h2><p>Run a review on the most recently updated open pull request.</p></div><Link to="/workflows">Manage <ArrowUpRight size={14}/></Link></header>{isLoading?<Skeleton/>:data?.active.length?<div className="execution-list">{data.active.map((workflow)=><Link to={`/workflows/${workflow.id}`} key={workflow.id}><span className="event-icon"><Github size={16}/></span><div><b>{workflow.name}</b><small>{workflow.repository}</small></div><button className="button" disabled={run.isPending} onClick={(event)=>{event.preventDefault();run.mutate(workflow.id);}}><Play size={14}/>Run now</button></Link>)}</div>:<div className="inline-empty"><Sparkles/><p>Connect GitHub and create a repository reviewer to get started.</p></div>}</section><aside className="panel connection"><header><div><h2>Recent executions</h2><p>Latest review activity</p></div><Link to="/executions">View all</Link></header>{data?.recent.length?<div className="execution-list">{data.recent.slice(0,3).map((row)=><Link to={`/executions/${row.id}`} key={String(row.id)}><div><b>{String(row.workflow_name)}</b><small>{new Date(String(row.started_at)).toLocaleString()}</small></div><Status value={String(row.status)}/></Link>)}</div>:<p style={{margin: "16px", fontSize: "13px"}}>No executions yet.</p>}<Link className="button wide" to="/integrations">Manage repositories <ArrowUpRight size={14}/></Link></aside></div></div>}
