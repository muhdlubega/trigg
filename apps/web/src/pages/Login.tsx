import { Github } from 'lucide-react';
import { useState } from 'react';
import { firebaseConfigured, login } from '../lib/api';
export function Login(){const [error,setError]=useState('');const signIn=(provider:'github'|'google')=>{setError('');void login(provider).catch((reason:unknown)=>setError(reason instanceof Error?reason.message:'Sign in failed'));};return <main className="auth-page"><a className="brand" href="/"><span className="logo-mark">T</span>Trigg</a><section className="auth-card"><div className="auth-flare"/><span className="kicker">WELCOME TO TRIGG</span><h1>Review pull requests automatically.</h1><p>Sign in, connect a repository, and let Trigg review every new pull request.</p><button className="button primary wide" disabled={!firebaseConfigured} onClick={()=>signIn('github')}><Github size={17}/>Continue with GitHub</button><button className="button wide" disabled={!firebaseConfigured} onClick={()=>signIn('google')}><span className="google">G</span>Continue with Google</button>{!firebaseConfigured&&<small>Firebase authentication has not been configured for this deployment.</small>}{error&&<small>{error}</small>}</section><div className="auth-aside"><div className="mini-flow"><span>PR OPENED</span><i/><span>FETCH DIFF</span><i/><span>AI REVIEW</span><i/><span>COMMENT</span></div><blockquote>“The automation layer between an event and what happens next.”</blockquote></div></main>}
